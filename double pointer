#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main()
{
    int* numPtr1;
    int** numPtr2;
    int num;

    scanf("%d", &num);

    numPtr1 = &num; //num을 할당
    numPtr2 = &numPtr1; //numPtr1을 할당

    printf("%d\n", **numPtr2);

    return 0;
}
